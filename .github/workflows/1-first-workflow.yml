name: First Workflow

on: [push]

jobs:
  first-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: First step
        run: |
          echo "1st step"
          echo "2nd step"
          echo "3rd step"
